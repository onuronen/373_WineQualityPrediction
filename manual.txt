Libraries:
numpy, pandas, matplotlib, sklearn
pip install numpy
pip install pandas
pip install matplotlib
pip install scikit-learn

To generate the graphs, run main.py under directory src, main.py under src/perceptron, main.py under src/svm
